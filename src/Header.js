import React from 'react';

const Header = () => {
    return (
      <header>
        <h1>IMemeMyTokens</h1>
        {/* Add navigation and other header elements here */}
      </header>
    );
  };

export default Header;
